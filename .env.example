# Backend
MUSIXMATCH_API_KEY=
JAMENDO_CLIENT_ID=
CATALOG_PATH=catalog/catalog.json
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Docker host ports (change if conflicts with other local apps)
POSTGRES_HOST_PORT=15432
API_HOST_PORT=8090
WEB_HOST_PORT=5190

# PostgreSQL (Docker service name: postgres)
DATABASE_URL=postgresql+psycopg://moony:moony@postgres:5432/moony

# Frontend — must match API_HOST_PORT
VITE_API_URL=http://localhost:8090
CORS_ORIGINS=http://localhost:5190,http://localhost:3000
